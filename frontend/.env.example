# Copy this file to .env and adjust if your backend runs elsewhere.
# Must match a backend URL and this dev server's own origin must be listed in
# the backend's CORS_ALLOWED_ORIGINS (see ../.env.example) — default Vite port is 5173.

VITE_API_BASE_URL=http://localhost:8000
